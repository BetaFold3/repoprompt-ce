# Plan: Oh My Pi (OMP) as a Third ACP Provider — Managed Barebones Harness

Scope: read when the task touches Oh My Pi/OMP provider integration, `ACPProviderID`/`ACPAgentProvider` seam extension for OMP, `Sources/RepoPrompt/Infrastructure/AI/Providers/OhMyPi/**`, or OMP Agent Mode wiring.
Authority: Authoritative
Last-verified: 2026-08-15

Status: **Phase B public enablement is implemented on top of Phase A resume readiness. OMP is supported in DEBUG and RELEASE for Agent Mode, MCP discovery/start, and Context Builder; window availability is connection-gated, with the DEBUG qualification lease retained as a strict OR override.**
Date: 2026-08-09
Provenance: an independent verification pass confirmed every load-bearing repository claim by direct reads and every upstream OMP claim against OMP source on `main` (can1357/oh-my-pi), then two independent Oracle plans (presets OracleB, OracleC) over a ~75k-token curated ACP-seam selection, one adversarial duel round on every material disagreement with referee-supplied source evidence, then synthesis. Preset identity (`model_preset_id`/`model_preset_name`) was verified on every send.

## Phase B public enablement (2026-08-14)

OMP is now present in the supported catalog and public Context Builder schema in all builds. Static/default fallback runtime availability is unavailable for OMP and therefore fails closed when no window settings context exists. Global Context Builder persistence preserves an explicit supported OMP agent/model selection without treating that stored choice as runtime readiness; startup and execution still resolve it against current-window connection and current-process verification. A successful current-window OMP connection enables Agent Mode, MCP `list_agents`/`agent_run`, and Context Builder; this window connection gating is authoritative, with the DEBUG qualification lease retained only as a strict OR override. Cached Context Builder restoration requires a current-process OMP verification, and OMP readiness is revalidated at UI launch and immediately before provider construction so cached state or revocation cannot start a runtime; the strict DEBUG qualification lease remains the only override. Context Builder mirrors the existing ACP model polling, subscription, workspace-reset, and window-close cleanup behavior. Dynamic discovered models plus the `Default` sentinel remain authoritative.

The public Context Builder smoke exposed one ordering race after successful OMP bootstrap: the outer bootstrap lease could observe the route and clean its waiter receipt before OMP's after-bootstrap pre-prompt barrier attached. Both an intact routed receipt and the unregistered-after-cleanup state now require a fresh authoritative live proof immediately before prompt dispatch: exact MainActor bidirectional run mapping, exact actor connection-to-run and connection-to-window mappings, active connection, admitted `omp-coding-agent` identity, retained run admission and expected-PID authority, and nonnil matching policy/cached windows. After the final MainActor mapping check, all actor-owned policy, identity, connection, admission, PID, and window receipts are sampled and validated again so reentrancy cannot accept revoked evidence. Failure, timeout, cancellation, wrong-client, inactive-connection, missing-PID, and window-mismatch evidence remain denied; no polling or delay was added.

The fresh installed-app rerun passed on 2026-08-15. In a dedicated scratch window, `app_settings` round-tripped `context_builder.agent=ohMyPi` and `context_builder.model=google-antigravity/gemini-3.6-flash`; Context Builder run `3FFFA783-BD29-4E05-AE6D-CA9E62C027EE` routed an exact admitted `omp-coding-agent` connection in window 3 and returned `public-no-qualification-route` from `OMP_PUBLIC_CONTEXT.md`. The hidden qualification lease was inactive before and after, the run terminated its child MCP connection, and settings were restored to `codexExec/default`. Private evidence is under `/private/tmp/omp-public-final.KRFSdM`.

Normal OMP starts require no hidden qualification lease in DEBUG or RELEASE. If a hidden DEBUG lease or context is supplied, every existing owner process, connection, workspace, generation, receipt, Apply Edits, and provider-identity check remains strict and fails closed; an explicitly supplied blank, non-string, or malformed lease value is rejected rather than treated as absence, and leases never authorize non-OMP targets. Expected-PID/pre-prompt routing, fixed managed flags, RepoPrompt-MCP-only tooling, bootstrap leases, and terminal cleanup ownership are unchanged. Recommendations, task-label ranking, and mutable generic provider settings remain out of scope.

## Phase A production resume readiness (2026-08-14)

OMP now selects ACP `session/load` for a nonblank persisted provider session ID and `session/new` otherwise, injecting the same RepoPrompt MCP configuration in either case. The headless bridge propagates `AgentMessage.resumeSessionID`; Agent Mode treats OMP as natively resumable and therefore does not replay transcript history after a successful load; `agent_manage.resume_session` no longer rejects OMP.

OMP is explicitly excluded from the generic ACP missing-session load-to-new fallback. Any OMP load failure remains an error and never opens a fresh session, because Agent Mode has already withheld transcript replay for native resume. Same-process controller reuse and the existing bootstrap lease, expected-PID routing, and terminal cleanup contracts are unchanged. Phase B subsequently enabled the public catalog, connection-gated availability, Context Builder, settings, and RELEASE start paths without changing these resume contracts.

Focused Phase A validation passed: `OhMyPiACPProviderTests` **25/25**, `ACPProviderSessionIdentityTests` **5/5**, `AgentManageMCPToolServiceResumeTests` **4/4**, agent-context checks, and `git diff --check`. This was focused validation, not a full test run.

The production public resume path then passed live on 2026-08-15 without a qualification lease. Session `E7AB6470-64C8-4D1E-9371-774291163928` completed an initial OMP turn, its workspace was unloaded so the first ACP/helper process pair exited, and `agent_manage.resume_session` rehydrated the persisted session. A follow-up started a distinct ACP/helper process pair and MCP connection under run `644BEA8A-C284-49F4-98F1-A12C610F767D`, completed exact expected-PID routing, and returned `OMP_RESUME_TURN3_OK:COBALT-742`, demonstrating provider-session continuity across process teardown and MCP re-registration. Cleanup deleted the synthetic MCP session and left the qualification lease inactive, no run process remaining, and Context Builder settings restored. Private evidence is under `/private/tmp/omp-public-final.KRFSdM`.

## Final six-gate live DEBUG qualification closure (2026-08-14)

All six live DEBUG gates passed. Gate 1 passed the poisoned workspace at `/private/tmp/omp-acp-run.q_u89op2`. Gates 2–3 passed exact allow and deny review plus visible `get_file_tree`/`apply_edits` transcript qualification at `/private/tmp/omp-qualification-evidence.Kz3cGG/omp-edit-qualification.9a0p03dz`. Gate 4 held one call for `600.0187964579673s`, then released, settled, and exited with zero in-flight calls and an unchanged workspace at `/private/tmp/omp-qualification-evidence.Kz3cGG/omp-acp-run.8bj66hww`. Gate 5 passed final cancel `/private/tmp/omp-qualification-evidence.Kz3cGG/omp-managed-cleanup-1c928dd4-fc90-42b4-a8c8-095f114058a9`, root kill `/private/tmp/omp-qualification-evidence.Kz3cGG/omp-managed-cleanup-265611b4-b83c-4984-ab27-467891fae7df`, helper kill `/private/tmp/omp-qualification-evidence.Kz3cGG/omp-managed-cleanup-7746e967-fb6d-4d65-b614-10d56c368b6b`, and independent verification `/private/tmp/omp-qualification-evidence.Kz3cGG/gate5-independent-final-1786680003`. The exact classifications were `exact-cancelled`, `exact-root-failure`, and `exact-completed-sentinel`, with connection counts `1/1/3`; every identity had strict OMP correlation, exactly one removal, zero raw in-flight calls/scopes, exactly one policy clear and expected-PID clear, restored session baseline, inactive lease, and no remaining OMP/helper processes. Gate 6 passed exact session/load with a second MCP registration at `/private/tmp/omp-acp-run.lqfrax9h`.

The final product corrections are that successful routed completion now performs policy-only clears, while terminal failure still revokes idempotently; DEBUG live-run reconnect mapping emits strict exact-once OMP identity evidence. Final focused validation passed: live-spike Python **100/100**, managed-cleanup Python **34/34**, apply-edits Python **6/6**, Python compilation, `MCPBootstrapLeaseTests` **11/11**, `MCPRunRoutingDiagnosticsTests` **9/9**, `AgentModeRunServiceLifecycleTests` **86/86**, RELEASE absence **1/1**, plus lint, build, and diff check. This was focused validation, not a full test run.

Phase B supersedes the former RELEASE-dark/public-availability restriction. The qualification evidence and cleanup results remain authoritative for the hidden DEBUG override path. Every dated pre-closure statement below that a live gate had not run, remained unproven, or remained outstanding/blocking is preserved as historical plan evidence and is superseded by this 2026-08-14 closure.

## Gates 1–3 bounded qualification-support checkpoint (2026-08-13)

The direct ACP spike now has a harness-owned-only `poisoned-workspace` phase. Against installed OMP 17.2.15 source, the encoded canary locations were confirmed as upstream discovery inputs: root `AGENTS.md`; `.agents/rules`, `.agents/skills`, and `.agents/AGENTS.md`; project `.omp/mcp.json`, `.omp/AGENTS.md`, `.omp/skills`, `.omp/extensions`, and `.omp/settings.json#extensions`. The phase uses the unchanged fixed managed argv, injects no MCP server, rejects ambient MCP audit, canary text/events, any tool or permission event, and non-exact sentinel output, and requires the descriptor-anchored workspace snapshot to remain unchanged from its fixed harness fixtures. Three focused hermetic poisoned-workspace tests and Python compilation passed; no live poisoned-workspace phase ran.

A one-shot apply-edits review exception is present only inside DEBUG qualification code. It is opt-in on the hidden start request, exact-lease/workspace/start-transaction bound before activation, exact-session/run/workspace bound for projection and response, and defaults off. Ordinary MCP control continues to force Auto Edit. The RELEASE source projection test proves the qualification request, gate methods, projection, response metadata, and override names are absent. The DEBUG-only response path exposes only the exact pending Apply Edits review with accept/reject and emits accepted/rejected response metadata.

`Scripts/qualify_omp_apply_edits_live.py` is an opt-in live scenario for an already-running caller-verified dedicated scratch-workspace window. Before creating its sole canary, it requires exact repeated scratch-path confirmation, exact single-root target-window identity, no live-window overlap, and an empty workspace. Its contract requires a visible `get_file_tree` transcript representation, accepted mutation only of the canary, rejected hash preservation, exact review metadata, exact sentinel, raw routing evidence, exact session cleanup, zero terminal raw in-flight calls, and inactive lease release. It contains no app launch/stop/relaunch or workspace-switch operation. Three hermetic script-contract tests passed. Focused `AppSettingsOMPSmokeGateTests` passed **27/27**, `AgentRunMCPToolServiceWaitTests` passed **40/40**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` passed **1/1**. The live edit scenario has **not** run, so gates 1–3 remain live qualification blockers; OMP remains RELEASE-dark, fresh-only, and resume-disabled.

## Historical implementation checkpoint (superseded by Phase B)

The dark implementation is now present in this worktree: identity/seam sweeps, the fixed launch resolver/provider/normalizer, fresh-only interactive and headless ACP paths, dynamic model polling/registry/catalog/menu plumbing, OMP-specific binding and RepoPrompt MCP grant, strict recognition-scoped duplicate approval handling, and a labeled Settings diagnostic/model-cache surface. Public availability remains false; OMP is not selectable and is excluded from Context Builder's public agent schema, generic binding/subagent Settings rows, recommendations, and task-label surfaces. A runtime guard additionally rejects restored or MCP-configured `.ohMyPi` sessions before workspace lookup or provider/controller startup while the dark flag is false. Headless OMP waits for registered MCP routing before the first prompt; the bootstrap lease retains cleanup ownership.

Local Phase-0 evidence now includes live, non-UI passes for all four direct phases of `Scripts/omp_acp_live_spike.py`: `cli-prompt`, no-tool ACP `prompt`, `bootstrap`, and `roundtrip`. The tested launch is `omp acp --no-tools --no-extensions --no-skills --no-rules --approval-mode yolo`; preflight separately verifies that the managed flags are global `omp --help` controls while `omp acp --help` identifies the ACP subcommand. The bundled `repoprompt-mcp` helper still initializes independently in the helper phase, and OMP accepts protocol-v1 initialize/authenticate/session-new with an injected stdio MCP server, returning `mode`, `model`, and `thinking` config IDs plus delayed `available_commands_update`/`session_info_update` notifications.

The passed `roundtrip` is intentionally synthetic transport evidence. It creates a fresh private scratch workspace, injects a test-only MCP proxy with exactly one read-only `get_file_tree` tool, requires its exact initialize/initialized/tools-list/tools-call sequence, snapshots the workspace before and after, and rejects permissions, unexpected inbound requests, unknown ACP update kinds, extra calls, non-empty arguments, and spoofed titles. OMP `17.2.12` called that tool once with empty arguments, emitted the observed raw title `mcp__repopromptce_get_file_tree`, received the synthetic result, and completed with the final round-trip sentinel. The proxy now has a hidden harness-only completion exit so it writes its audit before OMP process-group teardown; ordinary proxy tests retain EOF-based duplicate-request rejection.

A separate no-prompt live phase, `Scripts/omp_acp_live_spike.py --phase production-bootstrap`, also passed with evidence written under a private temporary directory. It injected the exact bundled `repoprompt-mcp` as the only ACP MCP server, captured one unique ready production connection, recorded authoritative `clientInfo.name` and normalized client ID `omp-coding-agent`, and proved that the bundled helper was a direct child/descendant of OMP. It observed no prompt, tool, permission, or unknown event, and the scratch workspace was unchanged. This is production transport, client-identity, and process-ancestry evidence only (`policyProof=false`): it does **not** prove that an app-owned run-scoped expected-PID policy was consumed.

The production `AgentProviderKind` MCP client hint is therefore `omp-coding-agent`; ACP `agentInfo.name` remains `oh-my-pi`. The live DEBUG smoke has now proved the app-owned ordered expected-PID/pre-prompt route for a fresh no-tool acknowledgement. Still unproven—and therefore release-blocking—are poisoned-workspace isolation, yolo suppression on real tool calls, server-side allow/deny enforcement without a duplicate prompt, visible tool-card presentation on real calls, an at-least-10-minute MCP call, cancellation/crash cleanup, and cross-process load with MCP re-registration. Capability advertisement alone is not resume proof: the implementation always creates a fresh session, ignores any candidate resume ID, and uses transcript handoff. No HTTP contingency or shared ACP DTO was added because stdio has not failed.

## Live app-owned qualification checkpoint (2026-08-13)

The corrected `Scripts/smoke_omp_agent_mode.sh` passed live end to end against the relaunched DEBUG app. Its fresh `agent_run` used the dynamic model sentinel `"default"`, completed with concrete model `anthropic/claude-opus-4-8`, and returned exact assistant text `OMP_AGENT_MODE_SMOKE_OK`. Stable target identity (window ID, workspace UUID, workspace instance, and repository roots) and tracked/index/existing-untracked content snapshots were unchanged. The smoke deleted only its synthetic MCP-created session (`deleted_count=1`, `skipped_count=0`); that session's exact helper connection produced exactly one terminal removed event with zero non-bookkeeping calls, zero raw non-bookkeeping calls, zero in-flight calls, and zero active scopes, and the qualification lease was inactive afterward.

The separately captured route history proved the app-owned ordered expected-PID/pre-prompt path, correlated the OMP ancestor with the bundled helper descendant by executable and process-start identity, verified client `omp-coding-agent`, and showed no run-correlated tool calls. Focused `OMPAgentModeSmokeScriptTests` passed **20/20** after the harness corrections; shell syntax, Python compilation, and diff checks passed. This closes the live DEBUG app-owned expected-PID/pre-prompt qualification blocker, but it is only a deterministic no-tool acknowledgement. It does **not** prove controlled RepoPrompt permission allow/deny without a double prompt, poisoned-workspace managed-flag isolation, visible tool-card behavior on real calls, ≥10-minute call tolerance, cancellation/crash cleanup, or cross-process load/re-registration/resume. Public RELEASE availability remains disabled; fresh-only remains; resume remains disabled.

A local `omp update` at 2026-08-13 11:00:04 +07 replaced the installed OMP entry with 17.2.15 files created around 11:00:07/08 and exposed a permission/mode hazard that required a manual executable-mode repair. This is local evidence, not a proven universal upstream defect. Availability support/preflight must fail closed with actionable repair guidance whenever the installed entry is not executable.

## Phase 5 availability-readiness checkpoint (2026-08-13)

The first reversible Phase 5 slice is implemented without changing the availability authority or any public projection. OMP preflight now distinguishes unavailable entries from installed-but-non-executable entries, provides install/PATH/absolute-path or executable-bit/reinstall guidance, revalidates cached executable identity fail-closed, and preserves the existing trusted ownership and permission checks without adding a fallback. Diagnostic model discovery now normalizes bootstrap failures through the OMP provider error authority; cancellation remains typed, the authentication matcher is limited to explicit auth phrases, and the canonical recovery tells users to open the OMP TUI and complete provider authentication.

Focused `OhMyPiACPProviderTests` passed **24/24**, `AppSettingsOMPSmokeGateTests` passed **25/25**, and coordinated lint/format checking passed with **0/1,820** files requiring formatting. The two new executable IDs are present in both the authoritative root listing and the curated ledger. The repository-wide ledger verifier still fails on unrelated broader drift (**286 missing, 3 stale**), with neither new OMP ID among the mismatches. No live qualification gate changed: poisoned-workspace isolation remains next, followed by controlled allow/deny without a double prompt, real-call tool cards, long-call tolerance, cancellation/crash cleanup, and cross-process load/re-registration. Public RELEASE availability remains disabled; fresh-only behavior, resume denial, and the dynamic `Default` sentinel are unchanged.

## Gate 6 cross-process-load harness checkpoint (2026-08-13)

Gate 6 now has an opt-in, test-only `cross-process-load` phase in `Scripts/omp_acp_live_spike.py`; it does not change provider runtime selection. Process A sends `session/new` with exactly one zero-tool MCP proxy and terminates without `session/close`. After preserving the session ID and first helper-registration audit, process B sends `session/load` with the byte-equivalent cwd/server payload, never falls back to `session/new`, requires a second exact helper registration, rejects ambiguous replay/registration/event ordering, and completes a bounded no-tool prompt. Both subprocesses retain the existing private evidence, disposable-workspace snapshot, process-group teardown, strict JSON-RPC, and terminal inbound-request safeguards.

Hermetic harness validation passed **86/86**. Focused `OhMyPiACPProviderTests` passed **25/25**, including the correction that an OMP `providerSessionID` cannot suppress transcript handoff or become a headless resume ID while OMP remains fresh-only. The new XCTest ID is present in the authoritative root list. Repository-wide ledger verification still reports the pre-existing unrelated drift (**286 missing, 3 stale**); the new OMP ID is not among those mismatches.

The live `cross-process-load` phase has **not** been run, so Gate 6 remains release-blocking live qualification rather than passed live evidence. OMP remains RELEASE-dark; interactive selection remains `session/new`; headless continues to discard `resumeSessionID`; resume remains disabled.

## Gates 4–5 qualification-support checkpoint (2026-08-13)

Gate 5 direct-spike support now includes a test-only held MCP `tools/call` mode with a separate immutable in-flight audit, an exact final settled audit, one accepted call, monotonic accepted/settled timestamps, harness-owned evidence-local release token/file, and explicit release observation. The live long-call phase is fail-closed unless the caller explicitly opts in, configures at least 600 monotonic seconds, and supplies a prompt timeout with a separate margin; ordinary spike and production timeout constants are unchanged. Isolated cancel, exact captured OMP-root SIGKILL, and exact captured proxy-child SIGKILL modes revalidate PID start identity immediately before signalling and retain process-group teardown/evidence handling.

A separate opt-in `Scripts/omp_managed_cleanup_qualification.py` targets only an already-running DEBUG app and requires an explicit absolute DEBUG CLI, exact window/workspace identity, exactly one caller-verified scratch root, a non-overlapping private evidence parent, and `--confirm-dedicated-scratch-window`. It never launches, stops, relaunches, switches workspaces, or targets by process name. Its cancel/root-kill/helper-kill scenarios require exact terminal state, exact connection removal and helper identity, zero terminal raw in-flight calls and active scopes, one policy clear and expected-PID clear, preservation of pre-existing/successor session IDs, the delivered-start lease remaining active and bound until exact release, exact synthetic-session deletion, and authoritative inactive lease status.

Hermetic scaled-duration validation passed for the held proxy/release and exact-PID signal primitives, and the separate managed-cleanup support tests passed **3/3** for dedicated-window rejection, routing/PID identity reconciliation, and terminal drain/policy/PID cleanup validation. Python compilation passed for the implementation and new cleanup harness. The shared `Scripts/test_omp_acp_live_spike.py` lane could not be re-run because a concurrent gate-1 addition currently has an invalid f-string at line 4563; this checkpoint does not claim that broader harness lane. No ≥600-second call, cancellation, process kill, bundled-route cleanup scenario, app lifecycle operation, or visible app interaction ran. Gates 4–5 therefore remain live qualification blockers; OMP remains RELEASE-dark, fresh-only, and resume-disabled.

## OracleA post-live-qualification remediation checkpoint (2026-08-10)

The later OracleA review requested changes; it supersedes the historical frozen-snapshot approvals below for the files changed afterward. The current worktree now enforces exact production-bootstrap identity `omp-coding-agent`, equality between the ready connection's trusted helper peer PID and the independently observed bundled-helper PID, and zero historical/in-flight tool calls or active scopes. Unknown ACP notifications are terminal. Explicit DEBUG CLI/environment overrides fail instead of falling back, and evidence records resolved-path provenance plus the CLI build identity. These stronger production-bootstrap assertions are covered by adversarial harness tests but have **not** been re-run against the live app/helper; they remain transport/identity evidence only and still set `policyProof=false`.

The former app-settings Boolean was removed. Its replacement is a hidden DEBUG-only exclusive process-owned lease that is bounded, expiring, non-persistent, one-shot, absent from RELEASE, and bound to the created fresh OMP session/run. Availability changes notify every window, while authorization is enforced independently at the actual provider-start boundary. Start/setup/benchmark/bind/response failure, expiry, and ownership mismatch fail closed; expiry/release cancel only the exact bound run. The shell qualification script uses bounded process-group-owned CLI calls, a dedicated qualification parameter, exact-run cancellation, raw pre-filter zero-tool accounting through terminal connection removal, bounded no-follow content snapshots of tracked/index/existing-untracked workspace state, and post-baseline ordered routing. Its PID proof distinguishes the launched OMP ancestor from the bundled helper descendant and correlates executable plus process-start identity. Synthetic tests cover success and adversarial cleanup/tool paths. No live app smoke, launch, stop, relaunch, or workspace interaction was performed.

## Safety-review/remediation checkpoint (2026-08-10)

The final frozen code-review snapshot is `2026-08-10/1714`. OracleA (`61D024A4-CCFB-4C9C-A614-CC65E0A1864C`) and OracleB (`7CDD523E-1D7C-47EA-98FE-D5FEA24D2D8C`) independently returned **“APPROVED — no P0/P1/P2 findings.”** This approval followed an iterative, adversarial cycle rather than a single-pass review. Its major turns fixed the dark generic headless-factory bypass; removed the production helper from OMP-facing bootstrap in favor of a discovery-only proxy while keeping `helper_preflight` independent; made inbound requests and permissions terminal; enforced strict pending/session/lifecycle/payload validation; added process-group and constructor rollback; changed filesystem inspection to descriptor-anchored no-follow traversal with actual-read budgets; added exclusive-file rollback and `BaseException` evidence preservation; and settled helper final-tail adjudication plus a capped tail diagnostic.

The final managed-harness suite passed **59/59**. Authoritative primary-session validation passed exactly: `py_compile`; the corrected executable/argv descendant scan with no findings; `git diff --check`; direct `Scripts/test_conductor_lifecycle.py` **142/142**; and the full `make conductor-selftest`, including harness **59/59** and lifecycle **142/142**. Worker-scoped lifecycle/watchdog failures were not reproducible in repeated primary clean-baseline runs and are not treated as an unresolved product regression; the repeated passing primary runs are the final authority.

Existing broader evidence remains: `AgentModeRunServiceLifecycleTests` **48/48** and the earlier full root **5,031/5,031**, with lint and build passing before the later focused settlements. The identity correction was subsequently covered by `OhMyPiACPProviderTests` **15/15**. This checkpoint does not complete the provider plan or change public availability: OMP remains dark/unavailable. At this snapshot, app-owned expected-PID/pre-prompt routing remained a manual gate; the 2026-08-13 checkpoint above closes only that qualification. Poisoned-workspace behavior, approval/deny handling, long calls, cancellation, visible tool cards, and resume remain release gates. No public availability flip, commit, or push is included in this documentation update.

The harness suite was **65/65** before `production-bootstrap`. The final closure enumeration contains **80** Python harness tests and **18** synthetic smoke/support tests. Final rerun results are recorded below after validation. The closure adds strict documented-path diagnostic envelope parsing, current executable file-identity revalidation, gitlink/nested-repository rejection, acquiring-connection and locally rejected-response lease cleanup, advertised exact-run cancel fencing, actionable-state transaction success, shared-gate test isolation, raw/canonical terminal tool-name evidence, and an in-process terminal connection-history ordering pin. None of this is live app evidence: `policyProof=false`, and no live smoke, launch, stop, relaunch, or workspace interaction was performed. The earlier `2026-08-10/1714` approval is historical evidence for its frozen snapshot; later edits require fresh review.

## 1. Goal

Add Oh My Pi as a third interactive ACP provider alongside OpenCode and Cursor, as a **managed barebones harness**: launch `omp acp --no-tools --no-extensions --no-skills --no-rules` plus a spike-confirmed approval-suppressing flag (`--approval-mode yolo` expected), inject the RepoPrompt MCP server through ACP `session/new` `mcpServers` as the **sole tool surface**, so users get OMP's broad model/auth ecosystem (Cursor Pro, OpenRouter, etc.) while RepoPrompt owns tools, context, and permissions. This is deliberately the opposite shape from the Pi native-RPC plan (`docs/context/plans/pi-provider-integration.md`): OMP rides the existing ACP seam; the two plans share only enum-sweep surfaces and must use distinct identifiers (`.ohMyPi` vs planned `.pi`).

## 2. Verified facts the plan rests on

Upstream OMP (source on `main`, verified 2026-08-09):

- `omp acp` is a first-party ACP server over stdio JSON-RPC (@agentclientprotocol/sdk). `initialize` advertises `loadSession:true`, `mcpCapabilities {http, sse}`, `promptCapabilities {embeddedContext, image}`, `sessionCapabilities {list, fork, resume, close}`.
- `#toMcpConfig` accepts **stdio** entries (`"command" in server`) plus http and sse — the advertised `mcpCapabilities` are the optional extended transports, not an exclusion of baseline stdio.
- ACP mode forces `enableMCP:false`: user global/project MCP config never loads; the ACP client is the exclusive MCP source.
- `--no-tools` sets `toolNames=[]` — an allowlist over **built-in tools only**. MCP tools bypass the allowlist entirely (name-dedup only) via `session.refreshMCPTools(manager.getTools())`. Under `--no-tools`, every tool call flows through injected MCP.
- `--no-extensions` sets `disableExtensionDiscovery=true`; `--no-skills` sets `skills=[]`; `--no-rules` sets `rules=[]`.
- configOptions advertises three selects: `mode`, `model` (`provider/model` IDs), and `thinking`. The local capture had thinking=`high` and options off/auto/minimal/low/medium/high; omission is intentionally not interpreted as auto.
- The acp-client-bridge always relays permission requests (`session/request_permission`); fs/terminal delegation happens only when the client advertises those capabilities.

Repository (verified by direct reads):

- `ACPProviderID` = {`openCode`, `cursor`}; the `ACPAgentProvider` protocol is the per-provider surface (support probe, launch config with `expectedExecutableIdentity`, session config with `mcpServers`, prompt blocks, normalization, error mapping).
- The shared `ACPAgentSessionController` owns lifecycle, JSON-RPC, `initialize` (advertises `fs.readTextFile:false`, `terminal:false`, no elicitation), `session/new|load` (serializes `mcpServers.map(\.acpJSONObject)` with `validateACPLaunchCommand` preflight), `session/request_permission`, cancellation, and modern configOptions (`session/set_config_option`, mode/model selectors, Cursor parameter selectors).
- **Cursor already injects RepoPrompt via `session/new` `mcpServers` in production** (`CursorACPAgentProvider.makeSessionConfiguration` returns `[repoPromptMCPConfiguration]`, `includeRepoPromptMCPServer` default true), stdio command-shaped. OpenCode instead uses the `OPENCODE_CONFIG_CONTENT` env overlay with `mcpServers: []`.
- `ACPDefaultSessionUpdateNormalizer` handles standard events; both existing providers delegate to it. `AgentACPModelRegistry` is keyed by `ACPProviderID` with `ACPDynamicModelStore` persistence; a new case flows through without registry changes. OpenCode has a headless ACP provider on the shared bridge.

## 3. Settled architecture (consensus of both plans)

- **Targeted core ACP provider** — no new package, plugin bridge, custom controller, or parallel runtime. OpenCode's file template for structure; **Cursor's session-config shape for MCP injection**.
- **Fixed, non-overridable managed launch profile** hard-coded in the config type: no settings toggle, no passthrough args, no per-flag opt-outs. The "managed" claim is gated by a behavioral poisoned-workspace test (canary AGENTS.md/rules, project MCP config, extensions incl. bundled defaults, skills must demonstrably have zero effect), not by trusting the flag list. An unclosable ambient input is a ship blocker escalated upstream.
- **For Agent Mode, RepoPrompt MCP is the sole tool surface and sole permission authority.** OMP-side approvals are neutralized (§4.4); RepoPrompt's server-side, fail-closed, per-profile MCP approval gates are authoritative. Oracle one-shots are a separate tool-free profile: RepoPrompt MCP is not injected, ACP approvals remain fail-closed, and a no-tools system suffix is always sent.
- **Dynamic models only**: configOptions → `AgentACPModelRegistry` → catalog, with a single `Default` sentinel as the only static entry; no OMP static model cases ever.
- New **`AgentProviderBindingID.ohMyPi`** with an independent RepoPrompt MCP grant and fixed managed-barebones permission profile; because that profile has no mutable choices, it does not manufacture a no-op secure permission document. Model-cache/diagnostic reset remains independent; no piggybacking on `.openCode`/`.cursor`.
- **Public availability gating**: OMP is a supported catalog provider in every build, while each window projects OMP into Agent Mode, MCP discovery/start, and Context Builder only after a successful OMP connection. In DEBUG, the exclusive process-owned qualification lease remains a strict OR override. The lease is default-off, process-local, non-persistent, bounded to at most 600 seconds, one-shot, absent from RELEASE, app-bound to the created session/run, and fail-closed on expiry, failed start, ownership mismatch, or undelivered start response. Lease notifications refresh every window's published availability, and a supplied lease/context never authorizes another provider.
- `requiresExpectedPIDOwnedAgentModeMCPRouting = true` and `requiresPrePromptAgentModeMCPRouting = true` — MCP-never-connected fails closed **before** the first Agent Mode prompt (load-bearing: MCP is that profile's entire tool surface). The headless route wait is conditional on `includeRepoPromptMCPServer`, so MCP-disabled Oracle requests do not wait on an impossible route.
- All changes additive (new enum raws, namespaced keys, no migrations); the availability flag is the single kill switch. Known rollback limitation: an older build won't recognize persisted `selectedAgent == "ohMyPi"`.

## 4. Design decisions (final, duel-settled)

### 4.1 MCP transport — stdio primary via the Cursor template (both lanes converged on referee evidence)

`OhMyPiACPAgentProvider.makeSessionConfiguration` returns `ACPSessionConfiguration(mode:, workingDirectory:, mcpServers: [mcpServer])` — the passed-in `RepoPromptMCPServerConfiguration` exactly as Cursor does (default server name preserved: `repoPromptToolName` recognition keys off it). Zero new transport DTOs; zero controller serialization changes; the path is production-exercised today. OracleB's "under-exercised first consumer" caution was retired and OracleC's "must be HTTP/SSE" position was withdrawn on source evidence (`#toMcpConfig` stdio support). **Contingency (designed, built only if the live spike falsifies stdio end-to-end):** a transport-tagged ACP MCP server DTO (`.stdio`/`.http`/`.sse`) pointing at RepoPrompt's existing HTTP endpoint, byte-for-byte preserving Cursor/OpenCode wire output.

### 4.2 Resume — production-ready and exact-or-error

A nonblank persisted provider session ID selects `session/load`; a blank or absent ID selects `session/new`. Both paths inject the same RepoPrompt MCP configuration. The headless adapter propagates `resumeSessionID`, and Agent Mode bypasses transcript replay only when native resume is selected. OMP load failure is provider-scoped fail-closed: unlike the existing generic ACP missing-session recovery for other providers, OMP never falls back to a fresh session after history replay has been withheld. This prevents silent transcript loss or duplication.

### 4.3 Thinking selector — deferred post-MVP; mechanism decided at fast-follow

MVP never sets `thinking`; the capture observed current `high`, so omission deliberately makes no claim about an `auto` default. Agreed constraints for the fast-follow, whichever mechanism wins: thinking is never encoded into the ACP wire model value sent to OMP; model is applied before thinking; unavailable values fail actionably without silent substitution; per-model persistence with a Default ("don't send") state. The duel crossed over on mechanism — OracleB ended favoring an additive `ACPRunRequest.additionalConfigOptionValues` map + separate `OhMyPiAgentToolPreferences` store; OracleC ended favoring reuse of Cursor's parameter-selector/bracket machinery (brackets as RepoPrompt-side selection encoding only). Decide with fixtures at implementation time; both are recorded as viable. Never map onto `ClaudeCodeEffortLevel`/`CodexReasoningEffort`. OMP's ACP `mode` select (default/plan) is unused: `acpSessionModeID` stays nil for all profiles.

### 4.4 Approvals — two belts, OMP-specific, with recognition scoping (OracleC conceded; OracleB refinement adopted)

Belt 1: the locally accepted yolo-equivalent flag is fixed in the profile, but suppression on real tool calls remains unproven and release-blocking. Belt 2: OMP's runtime permission binding auto-approves duplicate ACP tool-call permissions only when the protocol-level top-level tool name has an exact recognized RepoPrompt server prefix and canonical RepoPrompt tool name. Server identity inferred from untrusted `rawInput` is deliberately ignored; substring matches and anything unrecognized fall through to normal interactive handling and are logged. Rationale: with `--no-tools` + forced `enableMCP:false`, an ACP-layer prompt can only gate a call into RepoPrompt's independently enforced MCP policy — a strict duplicate — and a missing or renamed yolo flag must fail managed launch preflight rather than be absorbed by duplicate approval. Release is blocked unless the spike proves: no built-ins under the profile, ambient MCP cannot load, denied RepoPrompt MCP calls stay denied, auto-approval cannot bypass the server-side profile, headless runs don't stall on provider prompts.

### 4.5 Model menu — reuse `openCodeMenu`, parameterize before forking (OracleC conceded)

OMP raws are the same `provider/model` format; the `"opencode"` prefix-strip is inert for OMP inputs. Phase 3 adds adversarial menu tests over OMP-shaped inputs (`:free` suffixes, IDs resembling variant patterns, IDs without `/`, large catalogs, raw round-tripping); any misfiring heuristic gets gated by `providerID` inside the shared builder. Parallel OMP menu DTOs only as a proven-necessary fallback. No rename churn in this change.

### 4.6 Normalizer — thin named seam (OracleC conceded)

`OhMyPiACPEventNormalizer` delegates every payload to `ACPDefaultSessionUpdateNormalizer.normalize(_:providerID: .ohMyPi)`: template symmetry with both existing providers, a stable fixture-test anchor, and the future home for MCP tool-title canonicalization to `explicitRepoPromptToolName` if fixtures demand it. Stays pass-through otherwise.

### 4.7 Naming — referee ruling

Case `.ohMyPi`, raw `"ohMyPi"` for `ACPProviderID`/`AgentProviderKind`/`AgentProviderBindingID`, per the established camelCase identity convention (`openCode`'s raw is not its `opencode` binary name); `commandName "omp"`, `runtimeKind "omp_acp"`, display "Oh My Pi". OracleC's dissent (raw `"omp"`) recorded; identity ≠ executable name won.

## 5. Component inventory

New `Sources/RepoPrompt/Infrastructure/AI/Providers/OhMyPi/`: `OhMyPiAgentConfig` (fixed managed args, `CLIPathHints.ohMyPi`, `includeRepoPromptMCPServer`), `OhMyPiACPLaunchResolver` (OpenCode-shaped: probe ACP subcommand exit via `omp acp --help`, managed global flags via `omp --help`, plus a version pin, `ExecutableFileIdentity` capture/revalidation, and `AsyncMutex` probe serialization), `OhMyPiACPAgentProvider` (Cursor-shaped session config; OpenCode-shaped prompt blocks incl. images; actionable error mapping), `OhMyPiACPEventNormalizer`, `OhMyPiACPHeadlessAgentProvider` (shared ACP bridge, native resume propagation, `.declineUnsupported`; doubles as the model-refresh/discovery probe), and `OhMyPiCLIProvider` (fresh tool-free Oracle request through `HeadlessCLIStreamBridge`, warm-first canonical model validation, strict success terminal).

Enum/seam sweeps (all compiler-enforced arms plus an `rg` inventory for string-keyed surfaces in the PR description): `ACPProviderID`, `AgentProviderKind` (+every switch; verified MCP routing hint `omp-coding-agent`, while ACP `agentInfo.name` remains `oh-my-pi`), `AgentProviderBindingID`, `ACPAgentProviderFactory`, `AgentModelCatalog` (`ohMyPiAvailable` arms, ordering after `.cursor`, defaults/options/validation, recommendation-filter false), `AgentModel` arms, `AgentModeMCPToolPolicy.grantedTools` (standard ACP grant; **Phase-1 product decision**: inventory whether RepoPrompt MCP exposes a policy-gated exec tool — otherwise OMP is a read/search/edit harness at MVP, documented), permission resolver arm, CLI override/profile plumbing, settings UI block (probe, install guidance, managed-profile explanation, permission reset).

## 6. Phases

- **Phase 0 — Live spike + fixture capture (passed; all six live DEBUG gates closed 2026-08-14).** Preflight, direct helper, zero-tool bootstrap, CLI prompt, no-tool ACP prompt, the synthetic one-call read-only round trip, production bootstrap, the app-owned expected-PID/pre-prompt route, and the final six-gate qualification all passed. Raw evidence stays private and temporary; durable authority is this distilled result.
- **Phase 1 — Enum/seam sweep (implemented).** All identity, catalog, selection, binding, persistence, MCP-policy, and exhaustive/string-keyed arms are wired.
- **Phase 2 — Provider implementation + unit tests (implemented).** The OhMyPi directory, factory, fixed resolver/path hints, Cursor-shaped stdio session injection, normalizer fixtures, and focused contract tests are present. RepoPrompt server recognition deliberately requires the exact labels RepoPrompt writes for both permission identity and presentation canonicalization; a hand-renamed OpenCode/Cursor server entry may therefore lose RepoPrompt tool-card recognition rather than being accepted by substring. No HTTP contingency was justified.
- **Phase 3 — Interactive Agent Mode (implemented; live DEBUG qualification passed/closed).** Permission/controller policy and dynamic picker branches reuse `openCodeMenu`; production exact-or-error resume is enabled. Normal window availability is connection-gated in DEBUG and RELEASE. The hidden DEBUG qualification lease remains a strict OR override, but only its stable process-identity-verified owner can transactionally consume it for an OMP start; the provider-start boundary separately requires the bound authorization.
- **Phase 4 — Headless adapter + settings (implemented; live DEBUG qualification closed).** The shared headless bridge, native resume propagation, diagnostic probe/model refresh/reset surface, normal MCP discovery/start, and Context Builder construction are wired. Dynamic discovered models plus the Default sentinel remain authoritative.
- **Phase 5 — Live-gate hardening qualification complete.** All six live gates passed on 2026-08-14. The DEBUG qualification path remains strict and absent from RELEASE.
- **Phase B — Public enablement (implemented).** OMP is a supported catalog provider in every build; connection-gated Agent Mode, MCP, and Context Builder paths are enabled in DEBUG and RELEASE without requiring a hidden lease.
- **Phase 6 — Fast-follow (separately gated).** Thinking selector per §4.3.

Total ≈ 3–4.5 weeks, one engineer, including the spike.

### Next-session pickup (post-Phase B)

1. Treat the 2026-08-14 six-gate live DEBUG qualification and Phase B public enablement as complete; earlier dated dark/not-run statements are historical and superseded.
2. Preserve the connection-gated window availability authority, with the DEBUG qualification lease only as a strict OR override; do not add a parallel flag or fallback.
3. Preserve exact-or-error resume, the dynamic `Default` sentinel, current-process Context Builder verification, and all expected-PID/pre-prompt and cleanup contracts.
4. Keep recommendations, task-label ranking, thinking selection, and mutable generic OMP settings out of scope unless separately authorized.

## 7. Spike checklist (all with captured JSONL evidence)

1. **Production bootstrap and app-owned route qualification passed:** the sole injected bundled stdio `repoprompt-mcp` connected as `omp-coding-agent`; the live DEBUG smoke proved the ordered run-scoped expected-PID/pre-prompt path and OMP-ancestor/bundled-helper-descendant correlation with no run-correlated tool calls. This is a no-tool routing qualification, not tool-policy or tool-card proof. HTTP fallback is unnecessary while stdio continues to pass.
2. Poisoned-workspace managed-flag completeness (AGENTS.md/rules, project MCP, extensions incl. bundled defaults, skills; local config cannot override flags); enumerate managed global flags from `omp --help` (the 17.2.12 `omp acp --help` output is subcommand usage only) for missing ambient-input controls (`--no-rules` vs AGENTS.md-style context especially).
3. Approvals: default-mode `session/request_permission` frequency for MCP calls; exact yolo flag spelling/scope; suppression confirmed; denied RepoPrompt MCP calls stay denied; any non-tool permission kinds; no fs/terminal delegation.
4. `tool_call`/`tool_call_update` payload shapes for RepoPrompt MCP tools → normalizer fixtures; `repoPromptToolName` recognition renders RepoPrompt tool cards.
5. Long-blocking MCP call (≥10 min approval wait) vs OMP's MCP client timeout; identify the knob or its absence.
6. configOptions capture (mode/model/thinking IDs, defaults); mid-session `set_config_option` model; unset thinking harmless.
7. Cross-process `session/load`: MCP re-registration, history-replay shape vs controller dedup, prompt-after-load; `resume` vs `load` semantics. Gates §4.2.
8. Auth: `initialize` authMethods; logged-out error shape → error-mapping copy.
9. Lifecycle: cancel mid-generation/mid-MCP-call (stopReason, child cleanup), EOF/crash, usage-update shape or absence, cwd honoring, `omp --version`, install footprint for path hints.
10. Qualitative: a golden-path task under `--no-tools` — does the model drive RepoPrompt MCP tools competently?

## 8. Validation

- Unit suites under `Tests/RepoPromptTests/AgentMode/`: `OhMyPiACPLaunchResolverTests`, `OhMyPiACPEventNormalizerTests` (Phase-0 fixtures), `OhMyPiACPAgentProviderTests` (exact managed args; exactly one RepoPrompt `mcpServers` entry for both fresh `.new` and exact `.load` sessions and none for discovery; load failure never falls back to new; prompt blocks incl. images; error mapping), catalog/menu/selection-ID round-trip tests (raws contain `/`), registry persistence via test SPI, permission-binding and MCP-policy grant tests, headless bridge tests.
- Lanes: `make dev-test FILTER='OhMyPi'` during iteration; ACP regression lane (OpenCode/Cursor/controller suites) before/after Phases 2–4 to prove zero shared-controller drift; full `make dev-test` at each phase gate.
- Installed-app smoke (Phase 3 DEBUG, re-run at Phase 5 flip): streaming; RepoPrompt tool cards; approval allow **and** deny with no ACP-layer double prompt; long approval wait; steering mid-turn and mid-MCP-call; cancel with clean teardown; model picker after first session and after restart; model switch; resume across restart (if enabled); missing CLI / logged-out errors actionable; poisoned workspace inert; forced process kill → clean failed terminal; delegated `agent_run` (post-Phase 4).


### Current worktree validation (2026-08-10)

- Final frozen review snapshot: `2026-08-10/1714`; OracleA (`61D024A4-CCFB-4C9C-A614-CC65E0A1864C`) and OracleB (`7CDD523E-1D7C-47EA-98FE-D5FEA24D2D8C`) independently returned **“APPROVED — no P0/P1/P2 findings.”**
- Final harness: **59/59**. Primary-session checks passed: `py_compile`; corrected executable/argv descendant scan clean; `git diff --check`; direct `Scripts/test_conductor_lifecycle.py` **142/142**; full `make conductor-selftest`, including harness **59/59** and lifecycle **142/142**.
- Repeated primary clean-baseline runs did not reproduce worker-scoped lifecycle/watchdog failures. The passing primary runs are authoritative; the worker anomaly is not recorded as an unresolved product regression.
- Existing broader evidence remains: the earlier full root **5,031/5,031**, with lint/build passing, and the authoritative primary lifecycle/conductor validation recorded above. No new full-root run is claimed.
- The user independently passed preflight, helper, zero-tool bootstrap, `cli-prompt`, no-tool ACP `prompt`, the one-call synthetic read-only `roundtrip`, and the no-prompt `production-bootstrap`. Production bootstrap captured the exact bundled helper as the sole MCP server, unique ready connection identity `omp-coding-agent`, helper ancestry under OMP, no prompt/tool/permission/unknown event, and an unchanged scratch workspace. Its evidence boundary is transport/client identity/ancestry only (`policyProof=false`).
- Pre-push closure enumeration and focused validation: Python harness **80/80**; synthetic smoke/support **18/18**; `AsyncLimiterTests` **69/69**; `AppSettingsOMPSmokeGateTests` **12/12**; `AgentRunMCPToolServiceWaitTests` **17/17**; `AgentModeRunServiceLifecycleTests` **49/49**; `OhMyPiACPProviderTests` **18/18**; `AgentManageMCPToolServiceListAgentsTests` **2/2**; `AgentManageMCPToolServiceResumeTests` **4/4**; `ToolCatalogSnapshotTests` **8/8**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. A direct supported `RepoPrompt` RELEASE product build also passed; the three DEBUG-conditional array mutability warnings were absent, while unrelated pre-existing RELEASE warnings remain. The repository-wide ledger verifier was blocked by the shared worktree's broader mismatch (**273 missing, 3 stale**). `make guardrails` passed source-layout, contributor, license, and notice checks, then failed only the agent-context reachability check because `docs/context/plans/pi-provider-integration.md` is an unrelated orphan active document.
- Frozen follow-up snapshot `2026-08-11/1007` closure validation: Python harness **83/83**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **16/16**; `AgentRunMCPToolServiceWaitTests` **29/29**; `AgentModeRunServiceLifecycleTests` **52/52**; `AsyncLimiterTests` **69/69**; `AppSettingsWorktreeStartupBenchmarkTests` **1/1**; `AgentRunWorktreeStartTests` **39/39**; `WorktreeStartupInstrumentationTests` **22/22**; `AgentManageMCPToolServiceResumeTests` **4/4**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. The ledger contains the actual **22** added review-regression rows, and every test in each suite whose setup touches the singleton OMP gate declares that shared state and teardown reset ownership. The repository-wide ledger verifier still reports only the unchanged broader baseline (**273 missing, 3 stale**); the TSV has no snapshot, cleanup-disposition, or provenance columns, so no unsupported fields were invented. These focused results cover immutable token-matched start authorization and awaited receipt cleanup without cancelling successor/reacquired runs, terminalized post-dispatch failure paths, strict numeric process/start/parent-chain evidence, total routing shapes, hung-bootstrap session release, early explicit-OMP denial before availability preflight or routing, dual-token discard ordering, non-vacuous RELEASE projection, and recursive typed lifecycle rejection. An omitted-model request must first resolve the persisted target model and therefore reads the availability context before it can identify OMP, but it performs no spawn routing or target-session mutation before the effective-OMP lease check. The workspace guarantee is precise rather than absolute: the DEBUG runner revalidates the captured workspace and token-authorizes the immutable transaction, reports its receipt, and enters `controller.bootstrap()` through a mechanically adjacent observer/call sequence with no suspension between those steps; repository primitives cannot exclude a workspace change after control has entered the provider-owned asynchronous bootstrap/process-creation call. They are not new full-root or live-smoke evidence. No live app or production-bootstrap smoke ran, OMP remains production-dark/fresh-only/resume-disabled, and `policyProof=false` remains authoritative.
- Snapshot `2026-08-11/1132` closure validation: Python harness **83/83**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **18/18**; `AgentRunMCPToolServiceWaitTests` **29/29**; `AgentModeRunServiceLifecycleTests` **57/57**; `AgentRunWorktreeStartTests` **39/39**; `AgentManageMCPToolServiceListAgentsTests` **3/3**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. Eight new review-regression ledger rows were added and the first-qualification sentinel row was restored to `process_local_omp_lease;OhMyPiAgentModeSmokeGate.shared` plus `test_case+tearDown_reset`; the schema-grounded drift test pins that declaration. Receipt waits are cancellation-safe and multi-waiter-safe, service and queued-task early exits resolve denial, and the runner revalidates task/session/run/attempt/controller currency immediately before and after authorization. Weak-session cleanup is session-independent and its regression observes the authoritative bootstrap lease actor as released with routing and policy cleanup complete, alongside abandoned-attachment finalization and controller shutdown. The synthetic CLI audit now accepts only the exact `--raw-json -w <target> -c <tool> -j <payload>` grammar and rejects duplicate/unknown lifecycle, path, worktree, and external-path flags; the real-spawn helper test polls parsed decimal PID content, and the dead routing subset test was removed while the post-snapshot root invariant remains. Qualification-specific test hooks and diagnostics are DEBUG-only and source-backed by the RELEASE projection. The runner retains neutral authorization/reporter closure parameters in RELEASE because Swift conditional compilation cannot change one function parameter list without duplicating the overload/body; their qualification behavior, identifiers, flags, and state are absent from RELEASE, and the RELEASE product build plus projection passed. The repository-wide ledger verifier remains at the unrelated baseline **273 missing, 3 stale**. Format, lint, DEBUG app packaging, RELEASE `RepoPrompt` product build, shell syntax, Python compilation, context routing, and diff checks passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1245` closure validation: Python harness **84/84**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **20/20**; `AgentRunMCPToolServiceWaitTests` **30/30**; and `AgentModeRunServiceLifecycleTests` **60/60**. Receipt resolution now returns its effective one-shot outcome, waiter cancellation is local, and provider bootstrap requires an exactly accepted authorized receipt. Rollback waits are bounded and request-cancellation-shielded, use the released transaction snapshot as the authoritative bound-run fallback, and retain exact-run successor/reacquisition fencing. The runner repeats task/session/run/attempt/controller currency validation after bootstrap, so cancellation or controller replacement before that return cannot progress to ready or prompt and old resources are cleaned without touching a successor. A cheap DEBUG precheck rejects missing or workspace-stale qualification context before provider/controller/lease construction; the provider boundary remains authoritative. Production-bootstrap evidence now resamples every intermediate parent-chain PID tuple and link after executable inspection, repolls OMP before publication, and consistently emits the libproc parent PID. Positive non-overlapping routing preflight is covered, canonical numeric shell checks compare parsed values without string/int ambiguity, and the dark-selection ledger text reflects that a fully installed transaction can construct provider/controller before authoritative boundary denial while no provider process is created. The residual remains precise: repository primitives cannot prevent a workspace change after control enters provider-owned asynchronous bootstrap/process creation; the post-bootstrap currency fence covers cancellation/session/run/attempt/controller replacement, not an atomic workspace lock. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1349` closure validation: Python harness **84/84**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **20/20**; `AgentRunMCPToolServiceWaitTests` **31/31**; `AgentModeRunServiceLifecycleTests` **63/63**; `MCPBootstrapLeaseTests` **6/6**; `AgentRunWorktreeStartTests` **39/39**; `AgentManageMCPToolServiceListAgentsTests` **3/3**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. The runner now repeats its full startup-boundary snapshot after provider initialization completes and before later prompt suspension, and every current-owner cancellation path performs session-independent tool-tracking, attachment, bootstrap-lease, routing/policy, controller/task, and terminal cleanup without touching a successor. Both the cheap precheck and authoritative provider boundary require the transaction's exact MCP control session. Authorization uses one request-derived absolute deadline: its first expiry resolves denial immediately, and rollback reuses the remaining budget rather than opening another window. Provider-start failure instrumentation is centralized to one terminal event and one benchmark suffix with cancellation fallback. Production-bootstrap evidence rejects duplicate or cyclic PID chains, the synthetic-transaction ledger note now describes its actual provenance, failed bootstrap-lease acquisition cannot release another holder, and RELEASE retains the `.ohMyPi` fail-closed authorizer barrier with source-backed absence checks. The repository-wide ledger verifier still reports only the unrelated baseline **273 missing, 3 stale**; focused reconciliation covers the four new regression rows and corrected dark-selection provenance. Format, lint, DEBUG app packaging, RELEASE `RepoPrompt` product build, shell/Python syntax, context routing, and diff checks passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1444` closure validation: Python harness **84/84**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **21/21**; `AgentRunMCPToolServiceWaitTests` **33/33**; `AgentModeRunServiceLifecycleTests` **69/69**; `MCPBootstrapLeaseTests` **6/6**; `AgentRunWorktreeStartTests` **39/39**; `AgentManageMCPToolServiceListAgentsTests` **3/3**; `AgentManageMCPToolServiceResumeTests` **4/4**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. Provider-start authorization is now invoked on both fresh and reusable ACP paths, while the RELEASE service and runner retain independent fail-closed OMP barriers proven by source-backed projection. Authorization receipts enforce the request-derived absolute deadline and exact invocation `StartContext` identity, so expired or same-session replacement transactions cannot authorize bootstrap. Startup cancellation and failure share one joined teardown claim, central disposition distinguishes exact/detached ownership from successor replacement, terminal cleanup waits for bootstrap-lease release, and abandoned attachment deletion no longer depends on view-model lifetime. Missing and mismatched run identities have distinct terminal diagnostics/categories. Lifecycle tests now perform real singleton-gate setup/teardown isolation, and the ledger declares the deadline override and all nine added regression rows. The repository-wide ledger verifier remains at the unrelated baseline **273 missing, 3 stale**; focused reconciliation passed **9/9** new rows and the deadline-override declaration. Format, lint, DEBUG app packaging, RELEASE `RepoPrompt` product build, shell/Python syntax, context routing, and diff checks passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1554` OracleB follow-up validation: Python harness **84/84**; synthetic smoke/support **20/20**; `AppSettingsOMPSmokeGateTests` **22/22**; `AgentRunMCPToolServiceWaitTests` **33/33**; `AgentModeRunServiceLifecycleTests` **72/72**; and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. Prompt entry now atomically marks startup only when no teardown has been claimed; cancellation after readiness and all pre-prompt awaits joins the winning teardown and cannot submit `session/prompt`. Generic startup failure and cancellation use the same one-shot claim, with regressions pinning one terminal commit, one attachment finalization, one terminal lease-cleanup request, and complete routing/policy cleanup. A run-service entry rejection denies only the immutable invocation context, leaving a same-session replacement receipt independently authorizable. Direct task-local scope coverage pins the production context carrier across suspension. Four new ledger rows were reconciled, and every ledgered lifecycle test retains the singleton-gate shared-state/teardown declaration. The repository-wide ledger verifier remains at the unrelated baseline **273 missing, 3 stale**. Format, lint, DEBUG app packaging, RELEASE `RepoPrompt` product build, shell/Python syntax, context routing, and diff checks passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1636` final closure follow-up: the MCP start transaction now re-detects same-session invocation-context replacement during terminal cleanup, reclassifies the old failure as `qualification_context_replaced`, rolls back only the old exact transaction/run, and does not discard or terminalize the replacement target. `AgentModeRunService` separately denies only a mismatched immutable invocation receipt without mutating the shared replacement session. The production external MCP starter now revalidates the exact task-local qualification context immediately before instruction dispatch. The post-pre-dispatch race regression installs an authorized, bound successor context/run/attempt/controller, drives the old request through that production dispatch fence, and proves all successor identities and state survive old-request failure. The actionable start/wait regression also asserts the production task-local context at the injected `startRun` seam. The unused view-model attachment-finalizer instance method was removed while the value-owned finalizer remains. Observed focused results were Python harness **84/84**, synthetic smoke/support **20/20**, `AppSettingsOMPSmokeGateTests` **22/22**, `AgentRunMCPToolServiceWaitTests` **34/34**, `AgentModeRunServiceLifecycleTests` **72/72**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**; one new ledger row and the strengthened lifecycle row passed focused schema reconciliation **3/3**. DEBUG app packaging and the supported RELEASE `RepoPrompt` product build passed. The final source-validation rerun passed `AgentRunMCPToolServiceWaitTests` **34/34**, `AgentModeRunServiceLifecycleTests` **72/72**, `AppSettingsOMPSmokeGateTests` **22/22**, and the strengthened RELEASE projection **1/1**. Targeted SwiftFormat passed **7/7** touched Swift files and direct repository SwiftLint passed; the coordinated repository-wide format and lint targets were blocked at their SwiftFormat phase only by unrelated formatting drift in `WorkspaceCodemapArtifactDemandModels.swift`. The repository-wide ledger verifier completed test listing and reported the unchanged unrelated baseline **273 missing, 3 stale**. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/1807` closure validation: every DEBUG MCP start now installs a durable per-invocation generation that remains after transient OMP context clearing. All eligible destructive target cleanup paths use a MainActor compare-and-swap over the exact generation, control session, and optional authorized run immediately before discard; typed context-replaced and superseded outcomes preserve successor and reacquired transactions. Run-service and production external-starter entry fences deny only stale immutable receipts, including when a successor has already cleared its transient context. Lease-acquire failure, authorization denial, receipt rejection, cancellation, and generic startup failure share the joined startup teardown claim. Attachment cleanup falls back to the value-owned finalizer whenever the weak view model is gone while the session remains retained. `clearConsumedAttachmentsAfterProviderConsumption` is a stored immutable `let`, so the finalizer's captured policy cannot drift and no lazy-read seam is required. DEBUG terminal-cleanup accounting now records the one effective cleanup claim while retaining idempotent retry behavior. Five new review-regression ledger rows were added and the existing pre-denied row was strengthened for the cancellation race. Observed results were Python harness **84/84**, synthetic smoke/support **20/20**, `AppSettingsOMPSmokeGateTests` **22/22**, `AgentRunMCPToolServiceWaitTests` **36/36**, `AgentModeRunServiceLifecycleTests` **75/75**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. DEBUG app packaging, the supported RELEASE `RepoPrompt` product build, format check, and lint passed; the first sandboxed RELEASE attempt failed because Swift macro plugins were blocked, and the first unsandboxed retry was invalidated only by an unrelated concurrent shared-worktree file modification before the successful final retry. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/2027` remediation chunk 2A validation: live-harness qualification now admits only harness-created workspaces unless the explicitly audited `--unsafe-allow-unverified-workspace` waiver is supplied, and rejects workspace/repository overlap in either direction. Start-generation installation is discard-claim-aware, and cancellation-aborted discard clears only its exact claim while preserving successor ownership. Bootstrap-lease diagnostics separately record ordered raw cleanup requests and the single effective cleanup claim. Publication-only cancellation joins teardown only for the DEBUG OMP qualification marker with an unreleased startup lease; generic Cursor/OpenCode semantics remain publication-only, with bounded hung-teardown and joined-qualification regressions. The dark persisted/MCP-configured OMP regression now explicitly observes zero `controller.bootstrap()` entries. Observed results were Python harness **85/85**, `AppSettingsOMPSmokeGateTests` **22/22**, `AgentRunMCPToolServiceWaitTests` **38/38**, `AgentModeRunServiceLifecycleTests` **78/78**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. The repository-wide ledger verifier completed authoritative listing and retained the unrelated shared-worktree baseline (**273 missing, 15 stale**). Python compilation, shell syntax, repository format check, targeted SwiftFormat, and diff whitespace checks passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/2047` remediation chunk 2B validation: `ACPIntegratedAgentModeRunner` now has one typed terminal-state/agent-kind bootstrap-lease disposition policy shared by reusable and fresh retained-session teardown plus session-loss finalization. Startup-boundary checks use one synchronous typed stage/outcome revalidation helper and one invalid-boundary finalizer, preserving successful-path suspension timing and the intentionally distinct post-`providerInitializationCompleted` policy. Seven DEBUG runner closures are consolidated into one `OMPQualificationStartupProbes` value with no-op defaults; the RELEASE projection proves the seam absent while retaining both fail-closed authorizer calls and typed cancellation handling. Observed results were `AgentModeRunServiceLifecycleTests` **78/78**, `AppSettingsOMPSmokeGateTests` **22/22**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. Coordinated DEBUG packaging/build passed. The first sandboxed RELEASE compile was blocked by Swift macro-plugin sandbox denial; the approved unsandboxed retry built the `RepoPrompt` RELEASE product successfully. Repository format check and strict lint passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Snapshot `2026-08-11/2137` final 2101 correction validation: `AgentExternalMCPRunStarter` now always attempts activation/registration-owned control deactivation before rethrowing a post-activation qualification replacement; nil attachment reservations capture and own no session bookkeeping while explicitly captured files still finalize; and the ACP runner synchronously fences exact run, attempt, cancellation, and controller-slot ownership immediately after the `setExpectedMCPRunID` actor hop before publishing controller, terminal resources, or task state. The controller-local DEBUG suspension is carried through the single grouped qualification-probe value and is source-proven absent from RELEASE. Worktree-preparation failure now uses the same terminal qualification cleanup helper as later failures without changing the preceding instrumentation failure record or cancellation fallback. The dark-selection ledger oracle now explicitly states denial occurs before `controller.bootstrap()` while retaining the zero-bootstrap/process contract. Three observed regression rows were added. Focused results were `AgentRunMCPToolServiceWaitTests` **39/39**, `AgentModeRunServiceLifecycleTests` **80/80**, `AgentManageMCPToolServiceListAgentsTests` **3/3**, `AppSettingsOMPSmokeGateTests` **22/22**, and `WorktreeStartupBenchmarkReleaseAbsenceTests` **1/1**. Repository format check and strict lint passed. No live smoke or app lifecycle operation ran; OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`.
- Frozen snapshot `2026-08-11/2207` (**29 files**) final closure: terminal qualification cleanup preserves exact session context through the discard ownership CAS, records the benchmark suffix `[.failed, .discardRequested, .discardCompleted]`, and uses the effective failure category only for non-discarding outcomes; the released-view-model lifecycle ledger rows declare `OhMyPiAgentModeSmokeGate.shared` and `+tearDown_reset`, enforced class-wide. OracleA chat `omp-closure-2207-oraclea-E4CF84` on exact preset `61D024A4-CCFB-4C9C-A614-CC65E0A1864C` and OracleB chat `omp-closure-2207-oracleb-3B5ED7` on exact preset `7CDD523E-1D7C-47EA-98FE-D5FEA24D2D8C` both returned **“APPROVED — no P0/P1/P2 findings.”** Full `make dev-test-parallel` passed **509/509 suites** and **5,140/5,140 tests**; transient first-attempt retries for lifecycle and `RemoteAgentSession` both passed. Worker-scoped conductor lifecycle/watchdog anomalies repeated, while authoritative primary validation passed direct lifecycle **142/142** and `make conductor-selftest`: debug process **8/8**, OMP harness **85/85**, contribution preflight **11/11**, context checker **23/23**, CI runner **55/55**, conductor output **30/30**, benchmark **30/30**, lifecycle **142/142**, installer **22/22**, security **6/6**, and optimizer **53/53**. The DEBUG `RepoPrompt` product and supported RELEASE `RepoPrompt` product builds passed; the latter retained only unrelated existing warnings. Format passed **0/1,820**, strict lint passed, agent-context validation passed with **0 warnings**, and `git diff --check` passed. Direct safe live phases—preflight, helper, bootstrap, `cli-prompt`, prompt, and synthetic roundtrip—passed. OMP remains production-dark, fresh-only, resume-disabled, and `policyProof=false`; visible app-owned qualification gates remain.
- Live DEBUG qualification update (2026-08-13): the corrected `Scripts/smoke_omp_agent_mode.sh` passed end to end, including the fresh no-tool headless acknowledgement, app-owned ordered expected-PID/pre-prompt route, stable identity/content snapshots, exact synthetic-session deletion, terminal zero-tool accounting, and inactive lease. Focused `OMPAgentModeSmokeScriptTests` passed **20/20**; shell syntax, Python compilation, and diff checks passed. OMP remains dark/unavailable, fresh-only, and resume-disabled. Remaining release gates cover controlled approval allow/deny/no-double-prompt, poisoned-workspace isolation, real-call tool cards, long-call tolerance, cancellation/crash cleanup, and cross-process load/re-registration; resume remains disabled.

## 9. Risks

1. **Managed flags regress** (bundled extensions / AGENTS.md leak) — the poisoned-workspace gate passed live; any regression is a ship blocker to escalate upstream.
2. **OMP MCP client timeout regresses for long approval-gated calls** — the ≥600-second gate passed live; a future regression requires a knob, keep-alive progress notifications, an upstream issue, or a documented constraint.
3. **Tool-name/PID shape drift** can break RepoPrompt card pairing. Expected-PID/client/process correlation and visible real-call transcript/card behavior passed live for the qualified OMP shape (`omp-coding-agent`, OMP ancestor, bundled-helper descendant).
4. **Stdio unexpectedly rejected live** despite source support — designed HTTP contingency, ~2–3 days.
5. **Yolo flag missing/renamed in a future OMP** — managed launch preflight fails closed; scoped duplicate approval is not a substitute for the required flag.
6. **Young-project protocol drift** — min-version pin in `support(for:)`, default-normalizer tolerance, per-version fixture recapture.
7. **Non-compiler-enforced surfaces missed** — mandatory `rg` inventory in the Phase-1 PR.
8. **No shell without a RepoPrompt exec tool** — capability, not correctness; explicit Phase-1 inventory + product decision, documented limitation otherwise.
9. **Auth friction** (logins live in OMP's TUI) — MVP requires pre-authenticated OMP with actionable error copy.
